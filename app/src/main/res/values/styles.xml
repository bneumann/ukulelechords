<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorButton</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="ChordSelector" parent="android:TextAppearance.Material.Button">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/chord_select_text_size</item>
        <item name="android:background">@color/colorButton</item>
        <item name="android:textColor">@color/app_white</item>
        <item name="android:height">@dimen/chord_grid_element</item>
        <item name="android:padding">@dimen/menu_text_padding</item>
        <item name="android:layout_margin">@dimen/chord_element_margin</item>"

    </style>

</resources>
